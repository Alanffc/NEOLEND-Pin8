# fraud-detection

**Responsable:** Sergio Arias
**Puerto:** 8002
**Stack:** Python + FastAPI · PostgreSQL

Detección de fraude biométrico en tiempo real. **Data residency**: la biometría nunca sale del país (on-prem).

## Endpoints (TODO)
- `GET /health`

## Cómo correr
```bash
pip install -r requirements.txt && uvicorn app.main:app --reload --port 8002
```

> Esqueleto generado. Implementar la lógica en `app/`.
